#include <stdio.h>

/**
 * main -  writes program name
 * @argc: arguement count
 * @argv: array
 * Return: 0
 **/

int main(int argc, char *argv[])

{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
